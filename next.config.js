/** @type {import('next').NextConfig} */
const nextConfig = {
  //reactStrictMode: true
  // experimental: {
  //   productionServerSourceMaps: process.env.NODE_ENV !== 'production',
  // },
}

module.exports = nextConfig
